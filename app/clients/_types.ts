// ─── Shared types for the Clients feature ─────────────────────────
// Prefixed with _ so Next.js does NOT treat this as a route segment.

import { type Client } from '@/lib/api';

export type { Client };
